{
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite dev --host",
    "build": "vite build",
    "deploy": "echo .>> build.nojekyll && gh-pages -d build -t true"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^2.0.2",
    "@sveltejs/kit": "^1.15.10",
    "gh-pages": "^5.0.0",
    "svelte": "^3.58.0",
    "vite": "^4.3.4"
  },
  "dependencies": {
    "svelte-katex": "^0.1.2"
  }
}
